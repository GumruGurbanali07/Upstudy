@using CourseApp.Models
@model List<CourseCategory>
@{
    ViewData["Title"] = "Course Categories";
    Layout = "~/Areas/Admin/Views/Shared/_adminLayout.cshtml";
}

<div class="col-lg-12">
    <div class="ibox float-e-margins">
        <div class="ibox-title d-flex">
            <h5> Course Categories </h5>
            <a href="/Admin/CourseCategory/CreateCourseCategory" style="margin-left: 1100px" class="btn btn-primary">Add Category</a>

        </div>
        <!-- / ibox-title -->
        <div id="demo7" class="ibox-content collapse in">
            <div class="borderedTable">
                <div class="table-scrollable">
                    <table class="table table-striped table-bordered table-advance table-hover">
                        <thead>
                            <tr>
                                <th>Category Name </th>
                                <th>Status</th>
                                <th>Actions </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="highlight"><i style="margin-right:10px" class="fa-solid fa-@item.Icon"></i>@item.CourseCategoryName</td>


                                    <td>
                                        @if (item.IsActive)
                                        {
                                            <a href="/Admin/CourseCategory/ChangeStatusToFalse/@item.CourseCategoryId">
                                                <span class="badge badge-success">Active</span>
                                            </a>
                                        }
                                        else
                                        {
                                            <a href="/Admin/CourseCategory/ChangeStatusToTrue/@item.CourseCategoryId">
                                                <span class="badge badge-danger">Disactive</span>
                                            </a>
                                        }
                                    </td>
                                    <td>
                                        <a href="/Admin/CourseCategory/UpdateCourseCategory/@item.CourseCategoryId" class="btn btn-outline-primary btn-round btn-sm purple"> <i class="fa-solid fa-pen"></i> Edit </a>
                                        <a href="/Admin/CourseCategory/DeleteCourseCategory/@item.CourseCategoryId" class="btn btn-outline-danger btn-round btn-sm purple"> <i class="fa-solid fa-trash"></i> Delete </a>
                                    </td>
                                </tr>
                            }


                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

